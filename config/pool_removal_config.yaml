# 股票池移除策略配置
# 说明：配置哪些选股策略需要执行股票池移除，以及移除条件的参数
# 择时策略（TurtleStrategy、SupportStrategy等）不用于选股，不参与股票池移除
#
# 2026-09-11：min_hold_days 统一调整
#   - 资金流向规则：保持 1（入池第 1 天即检查，及时规避出货）
#   - 各选股策略趋势验证：统一为 5

# 资金流向移除规则全局配置
# 判定条件与「资金面一票否决」完全一致（直接复用 MoneyflowScorer.check_veto），两条件为 OR：
# 1. 5日主力净额 < -1亿 且 大单净流入占比 < -5%（无占比字段时回退：净额/成交额 < -1%）
# 2. 出货信号：大单净流出占比 > 1% 且 小单净流入占比 > 1%
# 命中后：不在冷却期 → 加入冷却期 3 天（不移除）；已在冷却期 → 直接移除出股票池
fund_flow_rules:
  is_enabled: true                    # 是否启用资金流向检查
  net_flow_threshold: -10000          # 【已废弃】阈值改由 MoneyflowScorer 一票否决常量统一控制
  min_hold_days: 1                    # 加入股票池多少天后开始检查（资金流向：入池第1天即检查）

removal_strategies:
  # 仙人指路策略
  ImmortalGuidanceStrategy:
    display_name: "仙人指路策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 阻力突破策略
  ResistanceBreakoutStrategy:
    display_name: "阻力位突破策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 主升低吸策略
  MainUptrendDipBuyStrategy:
    display_name: "主升低吸策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 底部反转策略
  BottomTrendInflectionStrategy:
    display_name: "底部趋势拐点"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # W底策略
  WBottomStrategy:
    display_name: "W底策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 多重金叉策略
  MultiGoldenCrossStrategy:
    display_name: "多金叉共振策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 多方炮策略
  MultiPartyCannonStrategy:
    display_name: "多方炮策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 强洗弱转强策略
  StrongWashWeakToStrongStrategy:
    display_name: "强势洗盘弱转强策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 趋势加速拐点策略
  TrendAccelerationInflectionStrategy:
    display_name: "趋势加速拐点"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 涨停横盘策略
  LimitUpSidewaysStrategy:
    display_name: "涨停横盘策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 晨星策略
  MorningStarStrategy:
    display_name: "启明星策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 涨停回马枪策略
  LimitUpPullbackStrategy:
    display_name: "涨停回马枪策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 2560战法选股策略
  Strategy2560Selection:
    display_name: "2560战法选股策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 趋势起点策略
  TrendStartStrategy:
    display_name: "趋势起点策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 金叉不绿策略
  GoldenCrossNotGreenStrategy:
    display_name: "金叉不绿策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 金三角策略
  GoldenTriangleStrategy:
    display_name: "金三角策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 超跌反弹策略
  OversoldReboundStrategy:
    display_name: "超跌反弹策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 低位九转策略
  LowTD9Strategy:
    display_name: "低位九转策略"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"

  # 龙头策略
  LeaderStrategy:
    display_name: "龙头策略"
    min_hold_days: 5
    is_enabled: true
    remark: "打板策略波动大，加入股票池5天后验证趋势"

  # 圆弧底策略
  BowlReboundStrategy:
    display_name: "圆弧底"
    min_hold_days: 5
    is_enabled: true
    remark: "加入股票池5天后验证趋势"
